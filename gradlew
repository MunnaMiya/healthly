#!/bin/sh
# Gradle wrapper script
# NOTE: This is a placeholder. Generate the real gradlew by running:
#   In Termux: cd /path/to/HealthlyProject && gradle wrapper
# OR copy gradlew and gradlew.bat from a fresh Android Studio "Empty Compose Activity" project.

echo "Please replace this file with the real Gradle wrapper."
echo "Run: gradle wrapper  (in Termux with gradle installed)"
echo "OR copy from a new Android Studio project."
exit 1
